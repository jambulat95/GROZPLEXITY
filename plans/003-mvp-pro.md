Мы переходим к финальному этапу бэкенда: **Level 3: Generator**.

Нужно добавить функционал генерации НОВОГО сценария на основе проанализированного стиля ("Passport").

### Задачи:

1. **Новый сервис `services/generator.py`**:
   - Функция `generate_script(style_passport: dict, topic: str) -> dict`.
   - Используй ту же модель Gemini 2.5.
   - **System Prompt**:
     "Ты — топовый сценарист Reels. Твоя задача — написать вирусный сценарий на тему '{topic}'.
     Ты ОБЯЗАН строго следовать стилю автора, описанному в JSON:
     {style_passport}
     
     Если у автора быстрый темп — пиши коротко. Если он шутит — шути.
     Структура сценария должна совпадать с 'structure' из паспорта.
     
     Формат вывода (JSON):
     {
       "title": "Заголовок видео",
       "script": [
         {"time": "00:00-00:03", "visual": "Описание кадра...", "audio": "Текст голоса..."}
       ],
       "viral_tips": "Советы как снять..."
     }"

2. **API Endpoint**:
   - Добавь POST `/api/v1/generate` в `api/routes.py` (или где у нас роуты).
   - Входные данные:
     ```json
     {
       "video_id": "папка_где_лежит_json_анализа",
       "topic": "Как я проспал работу"
     }
     ```
   - Логика:
     1. Найти JSON с результатами анализа (Level 2) по `video_id`.
     2. Вызвать `generate_script`.
     3. Вернуть результат.

Реализуй это.